// homology/src/boundary.rs
//! Producing boundary matrices of a SimplicialComplex.

use common::dense::DenseMatrix;
use common::Matrix;
use complex::SimplicialComplex;

fn boundary(complex: &SimplicialComplex) -> DenseMatrix<usize> {
    panic!()
}
